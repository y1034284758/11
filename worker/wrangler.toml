name = "capital-empire"
account_id = "15a5485f27b3dc810dfe1f0b73916dc6"
main = "src/index.js"
compatibility_date = "2024-11-01"

# 每个房间对应一个 Durable Object（RoomDO）。
# 房间状态持久化在 DO storage，WebSocket 负责实时推送。
[durable_objects]
bindings = [{ name = "ROOM", class_name = "RoomDO" }]

[[migrations]]
tag = "v1"
new_classes = ["RoomDO"]