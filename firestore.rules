rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{roomId} {
      // MVP: anonymous signed-in players may read/write a game room.
      // Use a server-authoritative rules engine before a public competitive launch.
      allow read, write: if request.auth != null;
    }
  }
}
